---
description: how to run the flutter app
---

1. Ensure a device is connected or an emulator is running.
2. Run `flutter pub get` to install dependencies.
3. Run `flutter run` to start the application.
